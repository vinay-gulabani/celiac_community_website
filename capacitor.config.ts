import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.celiac.community.app',
  appName: 'celiac_community_website',
  webDir: 'build'
};

export default config;
